import React from "react";

function Note() {
    return (
       <div className = "note">
           <h1>javascript and React.js</h1>
           <p>“You should imagine variables as tentacles, rather than boxes. They do not contain values; they grasp them—two variables can refer to the same value.” “You're building your own maze, in a way, and you might just get lost in it.” “jQuery is by far the most widely used library for JavaScript.</p>
       </div>
    );
}

export default Note;
